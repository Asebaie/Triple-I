# Классификация гиперспектральных данных

## Описание
Проект посвящен решению задачи семантической классификации пикселей гиперспектрального изображения. В рамках работы проведено сравнение классического ML-подхода (SVM с автоматическим подбором гиперпараметров через Optuna) и глубокого обучения (1D CNN). Особое внимание уделено оптимизации моделей и количественной оценке эффективности использования обучающих данных и параметров архитектуры.

## Функциональность
* **Анализ данных:** Визуализация спектральных кривых и анализ дисбаланса классов.
* **Классическое ML:** Обучение SVM с использованием `StandardScaler`, `PCA` и `Optuna` для поиска оптимальных параметров в зависимости от объема данных.
* **Deep Learning:** Реализация 1D CNN для классификации спектральных сигнатур.
* **Оценка эффективности:** Сравнение объема обучающих данных, необходимого для достижения целевого качества, и расчет выигрыша в числе обучаемых параметров по сравнению с 2D-моделями.

## Структура проекта
* `main.py`: Точка входа, оркестратор всех экспериментов.
* `config.py`: Глобальные настройки, пути и конфигурация логирования.
* `data_utils.py`: Функции загрузки, предобработки и визуализации данных.
* `svm_module.py`: Реализация пайплайна SVM, PCA и логика оптимизации гиперпараметров через Optuna.
* `cnn_module.py`: Архитектуры 1D CNN и Baseline 2D CNN, функции обучения и оценки.
* `log/`: Файл `execution.log` с подробной историей выполнения.
* `results/`: Сохраненные графики, метрики (CSV) и карты классификации.
