# R&D Knowledge Graph & Search Engine

## Описание

Этот проект представляет собой поисково-аналитическую систему и реляционный граф знаний R&D, разработанный для интеллектуального анализа научно-технических документов горно-металлургической отрасли. Цель системы — автоматизировать сбор разрозненных научно-технических отчетов (в форматах PDF и DOCX) в единую структурированную базу данных параметров на базе Sber GigaChat API и предоставить инструмент точного фактологического поиска с верификацией источников.

## Функциональность

*   **Пакетный асинхронный импорт (ETL):** Автоматический обход директорий, парсинг PDF (PyMuPDF) и DOCX (python-docx) файлов, нарезка текстов на перекрывающиеся чанки и извлечение ИИ структурированных технологических фактов и физико-химических параметров с валидацией через Pydantic.
*   **Умный гибридный поиск:** Декомпозиция сложных составных вопросов на подвопросы, трансляция естественного языка в SQL-запросы (Text-to-SQL) для точной математической фильтрации диапазонов и регистронезависимый поиск на кириллице (через дублирующие колонки в нижнем регистре).
*   **Fallback-поиск:** Автоматический запуск детерминированного поиска по корням слов прямо внутри SQLite при отсутствии точных совпадений или блокировках/таймаутах LLM-сервера.
*   **Аналитика и визуализация:** Интерактивный дашборд ключевых метрик базы знаний, визуальный анализатор пробелов (дефицита данных) и интерактивный сетевой граф связей между материалами, технологиями и файлами верификации.
*   **Информационная безопасность:** Суверенность (работа на серверах РФ через GigaChat API), защита от вредоносных SQL-инъекций на уровне бэкенда и покрытие автоматическими тестами безопасности.

## Структура проекта

*   **`api/`** — FastAPI бэкенд и маршрутизация поисковых запросов.
*   **`core/`** — Алгоритмическое ядро, ИИ-клиент, промпты извлечения и логика гибридного поиска.
*   **`db/`** — Подключение к СУБД SQLite, реляционные модели SQLAlchemy и логика сохранения данных.
*   **`parsers/`** — Извлечение сырого текста из документов и нарезка на чанки.
*   **`frontend/`** — Streamlit-чат, аналитические дашборды и рендеринг графа знаний.
*   **`scripts/`** — Пакетный импорт данных, утилиты нормализации и проверки базы данных.
*   **`tests/`** — Пакет тестов pytest на безопасность и валидацию данных.

---

## Как запустить проект локально

### Шаг 1. Настройка окружения
Пропишите в файл `.env` ваши ключи GigaChat API:
```env
GIGACHAT_CLIENT_ID=<YOUR_ID>
GIGACHAT_CLIENT_SECRET=<YOUR_SECRET>
GIGACHAT_SCOPE=GIGACHAT_API_PERS
```

### Шаг 2. Запуск проекта одной командой
Запустите скрипт автоматической сборки:
```bash
chmod +x run.sh
./run.sh
```

### Ручной запуск (альтернатива)
Если нужно запустить части проекта отдельно:

1. **Запуск импорта данных (ETL):**
```bash
python -m scripts.ingest_data
```
> **ВНИМАНИЕ:** Процесс первоначального импорта данных может занять длительное время из-за большого объема анализируемых документов.

2. **Запуск API бэкенда:**
```bash
uvicorn api.main:app --host 127.0.0.1 --port 8000
```

3. **Запуск фронтенда Streamlit (в новом окне терминала):**
```bash
streamlit run frontend/app.py --server.port 8501
```

Перейдите по адресу: **`http://localhost:8501`**

---

## Как запустить проект через Docker

Перед запуском убедитесь, что у вас установлен Docker и запущен Docker Desktop, а также настроен файл `.env`.

### 1. Сборка и запуск контейнеров
Запустите сборку и старт приложения одной командой в терминале:
```bash
docker-compose up --build
```

### 2. Использование приложения
Docker автоматически поднимет все компоненты системы. Перейдите по адресу:
**`http://localhost:8501`**

### 3. Остановка контейнеров
Чтобы остановить работу приложения в Docker, нажмите в терминале **`Ctrl + C`** или выполните команду:
```bash
docker-compose down
```

## Как запустить тесты

Перед запуском убедитесь, что активировано виртуальное окружение `(venv)` и установлены зависимости для тестирования:
```bash
pip install pytest pytest-asyncio sqlalchemy aiosqlite pydantic httpx
```

### 1. Запуск всех тестов
Выполните команду из корня проекта, чтобы запустить весь набор из тестов с подробным выводом:
```bash
pytest tests/ -v
```

### 2. Запуск тестов отдельного модуля
Чтобы протестировать только конкретную часть системы (например, только поисковый движок), укажите путь к файлу:
```bash
pytest tests/test_retriever.py -v
```

### 3. Запуск с детальным выводом ошибок
Если какой-то тест упал, используйте флаг `--tb=short` для получения компактного трейсбека с точной строкой падения:
```bash
pytest tests/ -v --tb=short
```

### 4. Тихий режим
Для быстрого прогона без лишнего вывода (только итоговая статистика PASS/FAIL):
```bash
pytest tests/ -q
```