# Предсказание сокристаллов — RASCAR Cocrystal Challenge

## Описание
Проект представляет собой решение хакатона [RASCAR Cocrystal Challenge](https://www.kaggle.com/competitions/rascar-cocrystal-challenge/overview), организованного [RASCAR AI](https://t.me/rascar_ai).

Задача — **бинарная классификация**: по паре молекул в формате SMILES предсказать, образуют ли они сокристалл (`result = 1`) или нет (`result = 0`). Сокристалл — кристаллическая форма из двух разных молекул, объединённых нековалентными взаимодействиями (водородные связи, π-стэкинг, ван-дер-ваальсовы силы).

## Функциональность

* **Поиск точных совпадений:** перед обучением скрипт проверяет, встречались ли тестовые пары в трейне, и подставляет известный ответ напрямую.
* **Молекулярные фингерпринты:** вычисление ECFP4, ECFP6, MACCS keys и RDKit FP для каждой молекулы.
* **Попарные признаки:** поэлементное произведение, разность, Tanimoto-сходство, H-bond комплементарность и нелинейные комбинации дескрипторов.
* **Отбор признаков:** удаление низковариативных признаков через `VarianceThreshold`.
* **Обучение модели:** LightGBM с 10-фолдовой стратифицированной кросс-валидацией и `scale_pos_weight` для борьбы с дисбалансом.
* **Тюнинг порога:** автоматический подбор порога классификации по сетке для максимизации Weighted Accuracy.

## Структура проекта

* `main.py`: основной скрипт — признаки, обучение, предсказание, сохранение результата.
* `rascar-ai-chem-hack/train.csv`: обучающая выборка (пары SMILES + метка).
* `rascar-ai-chem-hack/test.csv`: тестовая выборка (пары SMILES без метки).

---
*Тестовый датасет содержит 1000 пар. Порог классификации подбирается автоматически на OOF-предсказаниях.*
